#include <iostream>
#include <unex.h>
#include <vector>

int main() {

    // HeapArray<int> arr(1);
    // int* pint = new int(5);
    // arr.Add(56);
    // arr[0] = pint;
    // int* test = arr[0];
    // std::cout << sizeof(Allocator<int>) << std::endl;

    return 0;
}
